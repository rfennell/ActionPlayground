describe("A basic test", () => {

    it("should be able to add two numbers", () => {
      // arrange

      // act
      var actual = 1 + 1;

      // assert
      expect(actual).toBe(2);
    });
})